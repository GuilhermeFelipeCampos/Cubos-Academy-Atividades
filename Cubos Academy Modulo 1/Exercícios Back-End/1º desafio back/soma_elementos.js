const array = [55, 4, 548, 3, 6, 8, 9];

let min = [];
//const soma = array.reduce((soma, array) => soma + array, 0);

for (let item of array) {
    if (item >= 18) {
        min.push(item);
    }

    replace(/[\.,-\/#!$%\^&\*;:{}=\-_`~()@\+\?><\[\]\+]/g, '')
}
array2 = "Caraca, não consigo separar esta merda de string"