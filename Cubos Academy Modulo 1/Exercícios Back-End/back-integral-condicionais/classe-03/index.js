const ladoA = 3;
const ladoB = 3;

//seu código aqui

if (ladoA === ladoB){

    console.log(`Sim com ${ladoA} e ${ladoB}`);

}else{
    console.log("Não e " + (ladoA > ladoB ? `O lado A é maior com ${ladoA}` : `O lado B é maior com ${ladoB}`));
}